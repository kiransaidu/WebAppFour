@model IEnumerable<WebAppFour.Models.BookCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Friction)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comedy)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Friction)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comedy)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Drama">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Drama">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Drama">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
